# Capacitor Setup Guide

## ⚠️ Требования

Перед началом работы убедитесь, что установлены необходимые инструменты:

### Для Android разработки:
- ✅ **Android Studio** с Android SDK
- ✅ Настроенные переменные окружения (`ANDROID_HOME`)
- ✅ Java Development Kit (JDK) 17 или выше

**📖 Подробная инструкция:** См. файл `ANDROID_SETUP.md`

### Для iOS разработки:
- ✅ **macOS** (только на Mac можно собирать iOS приложения)
- ✅ **Xcode** 14 или выше (из App Store)
- ✅ **CocoaPods** (`sudo gem install cocoapods`)
- ✅ Xcode Command Line Tools (`xcode-select --install`)

## Быстрый старт

### 1. Добавьте платформу для разработки

```bash
# Для iOS (требуется macOS с установленным Xcode)
npm run cap:add:ios

# Для Android (требуется установленный Android Studio)
npm run cap:add:android
```

### 2. Соберите приложение и синхронизируйте

```bash
npm run build
npm run cap:sync
```

### 3. Откройте в нативной IDE

```bash
# iOS
npm run cap:open:ios

# Android
npm run cap:open:android
```

### 4. Запустите приложение

- **iOS**: Используйте Xcode для запуска на симуляторе или устройстве
- **Android**: Используйте Android Studio для запуска на эмуляторе или устройстве

## Установленные плагины

- `@capacitor/app` - Базовые функции приложения (закрытие, внешние ссылки и т.д.)
- `@capacitor/device` - Информация об устройстве
- `@capacitor/haptics` - Тактильная обратная связь
- `@capacitor/keyboard` - Управление клавиатурой
- `@capacitor/splash-screen` - Экран загрузки
- `@capacitor/status-bar` - Полоса состояния

## Live Reload для разработки

Для разработки с live reload:

1. Откройте `capacitor.config.ts` и раскомментируйте секцию `server`:

```typescript
server: {
  url: 'http://localhost:5173',
  cleartext: true,
},
```

2. Запустите dev сервер:
```bash
npm run dev
```

3. Синхронизируйте:
```bash
npm run cap:sync
```

4. Запустите приложение из Xcode/Android Studio

Ваши изменения в коде будут автоматически отображаться в приложении!

## Важные команды

### Быстрый запуск (всё в одном)
- `npm run android` - Собрать + синхронизировать + открыть Android Studio
- `npm run ios` - Собрать + синхронизировать + открыть Xcode

### Отдельные команды
- `npm run cap:sync` - Синхронизировать изменения в нативные проекты
- `npm run cap:copy` - Копировать только bundle без синхронизации конфигурации
- `npm run cap:open:ios` - Открыть iOS проект в Xcode
- `npm run cap:open:android` - Открыть Android проект в Android Studio

## Конфигурация

Файл конфигурации: `capacitor.config.ts`

Здесь вы можете настроить:
- App ID
- Имя приложения
- Внешний каталог (webDir: `dist/client`)
- Параметры сервера для разработки
- Настройки плагинов

## Документация

Полная документация Capacitor: https://capacitorjs.com/docs

