# Установка Android Studio и SDK

> 💡 **Совет:** После установки запустите `npm run check:android` для проверки настройки окружения

## Шаг 1: Скачайте Android Studio

🔗 **Официальный сайт:** https://developer.android.com/studio

Или прямая ссылка на скачивание: https://developer.android.com/studio/install

## Шаг 2: Установка Android Studio

1. Запустите скачанный установщик
2. Следуйте инструкциям мастера установки
3. **Важно:** При установке выберите опцию **"Android SDK"** (обычно выбрана по умолчанию)
4. Также убедитесь, что установлен **Android SDK Platform Tools**

## Шаг 3: Настройка SDK

После установки Android Studio:

1. Откройте Android Studio
2. На экране приветствия нажмите **"More Actions" → "SDK Manager"**
   (или в открытом проекте: `Tools → SDK Manager`)

3. Во вкладке **"SDK Platforms"** установите:
   - ✅ Android 13.0 (Tiramisu) или выше
   - ✅ Android 12.0 (S) 
   - ✅ Android 11.0 (R)

4. Во вкладке **"SDK Tools"** убедитесь, что установлено:
   - ✅ Android SDK Build-Tools
   - ✅ Android SDK Platform-Tools
   - ✅ Android Emulator
   - ✅ Android SDK Command-line Tools

5. Нажмите **"Apply"** и дождитесь завершения установки

## Шаг 4: Переменные окружения (Windows)

После установки нужно настроить переменные окружения:

### Автоматическая настройка (PowerShell от администратора):

```powershell
# Путь по умолчанию, проверьте свой путь!
$ANDROID_HOME = "$env:LOCALAPPDATA\Android\Sdk"

# Установка переменных окружения
[System.Environment]::SetEnvironmentVariable("ANDROID_HOME", $ANDROID_HOME, "User")
[System.Environment]::SetEnvironmentVariable("ANDROID_SDK_ROOT", $ANDROID_HOME, "User")

# Добавление в PATH
$currentPath = [System.Environment]::GetEnvironmentVariable("Path", "User")
$newPaths = @(
    "$ANDROID_HOME\platform-tools",
    "$ANDROID_HOME\cmdline-tools\latest\bin",
    "$ANDROID_HOME\emulator"
)

foreach ($path in $newPaths) {
    if ($currentPath -notlike "*$path*") {
        $currentPath = "$currentPath;$path"
    }
}

[System.Environment]::SetEnvironmentVariable("Path", $currentPath, "User")

Write-Host "✅ Переменные окружения настроены!" -ForegroundColor Green
Write-Host "⚠️ Перезапустите PowerShell/Terminal для применения изменений" -ForegroundColor Yellow
```

### Ручная настройка:

1. Откройте **"Параметры системы"** → **"О системе"** → **"Дополнительные параметры системы"**
2. Нажмите **"Переменные среды"**
3. В разделе **"Переменные пользователя"** нажмите **"Создать"**:
   - Имя: `ANDROID_HOME`
   - Значение: `C:\Users\ВАШ_ПОЛЬЗОВАТЕЛЬ\AppData\Local\Android\Sdk`
4. Найдите переменную **"Path"** и нажмите **"Изменить"**
5. Добавьте следующие пути:
   - `%ANDROID_HOME%\platform-tools`
   - `%ANDROID_HOME%\cmdline-tools\latest\bin`
   - `%ANDROID_HOME%\emulator`

## Шаг 5: Проверка установки

**Перезапустите PowerShell/Terminal** и выполните:

### Автоматическая проверка (рекомендуется):
```bash
npm run check:android
```

Этот скрипт проверит все необходимые компоненты и покажет, что нужно исправить.

### Ручная проверка:
```powershell
# Проверка Android SDK
adb --version

# Проверка переменных окружения
echo $env:ANDROID_HOME

# Проверка Java
java -version
```

Если команды работают — всё готово! ✅

## Шаг 6: Создайте эмулятор Android

1. В Android Studio откройте **"More Actions" → "Virtual Device Manager"**
   (или `Tools → Device Manager`)
2. Нажмите **"Create Device"**
3. Выберите устройство (например, **Pixel 6**)
4. Выберите образ системы (рекомендуется последний API Level)
5. Скачайте образ, если нужно
6. Завершите создание эмулятора

## Шаг 7: Теперь можно запускать проект!

После установки и настройки:

```bash
# Добавить Android платформу
npm run cap:add:android

# Собрать и открыть проект
npm run android
```

## Возможные проблемы

### "adb не является внутренней или внешней командой"

Это значит, что PATH не настроен правильно. Проверьте переменные окружения и перезапустите терминал.

### "Could not find or load main class"

Убедитесь, что установлены **Android SDK Command-line Tools** в SDK Manager.

### Эмулятор не запускается

Убедитесь, что:
- Включена виртуализация в BIOS (Intel VT-x / AMD-V)
- Установлен Android Emulator в SDK Tools

## Полезные ссылки

- 📚 Официальная документация Android Studio: https://developer.android.com/studio/intro
- 📚 Capacitor Android Development: https://capacitorjs.com/docs/android
- 🔧 Решение проблем: https://capacitorjs.com/docs/android/troubleshooting

