# Firebase Auth - Быстрый Старт

## Шаги для запуска авторизации

### 1. Установите зависимости

```bash
npm install
```

Это установит все необходимые пакеты, включая `@hono/firebase-auth@^1.4.2`.

### 2. Создайте KV namespace для кеширования публичных ключей

```bash
npx wrangler kv:namespace create PUBLIC_JWK_CACHE_KV
```

Команда выдаст ID вида:
```
{ binding = "PUBLIC_JWK_CACHE_KV", id = "abc123def456..." }
```

### 3. Обновите wrangler.jsonc

Откройте `wrangler.jsonc` и замените `"id": "placeholder"` на реальный ID из предыдущего шага:

```jsonc
"kv_namespaces": [
  {
    "binding": "PUBLIC_JWK_CACHE_KV",
    "id": "ваш-реальный-id-здесь"
  }
]
```

### 4. Включите Email/Password авторизацию в Firebase Console

1. Перейдите на https://console.firebase.google.com/
2. Выберите проект **planer-8edbd**
3. Откройте **Authentication** → **Sign-in method**
4. Включите провайдер **Email/Password**

### 5. Запустите проект

```bash
npm run dev
```

### 6. Тестирование

1. Откройте приложение в браузере
2. Вы увидите экран входа
3. Создайте новый аккаунт (Sign up)
4. **Проверьте email** - вам придет письмо с ссылкой для подтверждения
5. Кликните на ссылку в письме для подтверждения email
6. Вернитесь в приложение и нажмите "I Verified My Email"
7. После подтверждения вы войдете в систему
8. Попробуйте кнопку "Получить данные" - она сделает защищенный API запрос

## Что реализовано

### Frontend (React + Firebase)
✅ Firebase конфигурация (`src/firebase.ts`)
✅ AuthContext для управления состоянием (`src/AuthContext.tsx`)
✅ Компонент Login (`src/components/Login.tsx`)
✅ Компонент Register (`src/components/Register.tsx`)
✅ Компонент VerifyEmail (`src/components/VerifyEmail.tsx`)
✅ Интеграция в App.tsx с проверкой авторизации
✅ **Обязательная проверка email перед доступом к приложению**
✅ Автоматическая отправка письма верификации при регистрации
✅ Возможность повторной отправки письма верификации
✅ Автоматическое сохранение JWT токена в localStorage
✅ Автоматическое обновление токена каждые 50 минут

### Backend (Cloudflare Workers + Hono)
✅ Middleware для проверки Firebase JWT токенов
✅ **Middleware для проверки email верификации (emailVerified)**
✅ Публичные endpoints (health, version, versions)
✅ Защищенные endpoints (/api/protected/*)
✅ Автоматическая валидация токенов
✅ Отклонение неподтвержденных email (403 Forbidden)
✅ Кеширование публичных ключей в KV

## API Endpoints

### Публичные (без авторизации)
- `GET /api/health` - проверка работоспособности
- `GET /api/version` - текущая версия приложения
- `GET /api/versions` - список всех версий

### Защищенные (требуют JWT токен)
- `GET /api/protected` - пример защищенного endpoint
- `GET /api/protected/profile` - профиль пользователя из токена

### Пример защищенного запроса

```typescript
const token = localStorage.getItem('firebaseToken');
const response = await fetch('/api/protected/profile', {
  headers: {
    'Authorization': `Bearer ${token}`,
  },
});
const data = await response.json();
console.log(data); // { uid, email, emailVerified, name, picture }
```

## Безопасность

🔒 **Пользователи БЕЗ подтвержденного email НЕ МОГУТ войти в приложение**
🔒 **Backend проверяет emailVerified в каждом запросе к защищенным endpoints**
🔒 Пользователи без авторизации не могут получить доступ к защищенным endpoints
🔒 Просроченные токены автоматически отклоняются
🔒 JWT токены хранятся в localStorage
🔒 Токены автоматически обновляются каждые 50 минут (срок действия 1 час)
🔒 Все защищенные API endpoints требуют валидный токен в заголовке `Authorization: Bearer <token>`
🔒 Письма верификации отправляются автоматически при регистрации
🔒 CORS настроен с поддержкой credentials

## Troubleshooting

**Ошибка: Cannot find module '@hono/firebase-auth'**
→ Запустите `npm install`

**Ошибка: KV namespace not found**
→ Создайте KV namespace и обновите ID в wrangler.jsonc

**Login не работает**
→ Проверьте, что Email/Password включен в Firebase Console

**401 Unauthorized на защищенных endpoints**
→ Проверьте, что токен передается в заголовке Authorization

**403 Forbidden - Email not verified**
→ Email не подтвержден. Проверьте почту и кликните на ссылку верификации

**Письмо верификации не приходит**
→ Проверьте папку "Спам" или "Промоакции"
→ Используйте кнопку "Resend Verification Email"

